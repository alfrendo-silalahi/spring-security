spring:
  application:
    name: spring-boot-jwt
  datasource:
    url: jdbc:postgresql://localhost:5432/spring_boot_jwt
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format-sql: true
    database: postgresql
    database-platform: org.hibernate.dialect.PostgreSQLDialect

logging:
  level:
    root: DEBUG

jwt:
  secret-key: bdf2d6b34a06ab6a7beb5fd89039a01a962e946d6b38aaafab3299f8187951d8
